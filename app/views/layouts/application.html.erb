<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Tinder</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <p class="alert"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  
    <nav class="navbar navbar-default navbar-fixed-top" style="background-color: #bfffe3;">
      <div class="container-fluid">
        
         <!--Brand image -->
        <div class="navbar-header">
          <a class="navbar-brand" href="#">
          <img alt="Tinder" src="...">
          </a>
        </div>
        <!--Text-->
        <% if user_signed_in? %>
          <p class="navbar-text">Signed in as <%= current_user.email %> | <%= link_to 'New Post', new_post_path %> </p>
          <p class = "navbar-text navbar-right">Not you?  <%= link_to "Sign Out", destroy_user_session_path, :method => :delete  %></p>
        <% else %>
          <p class="navbar-text"><%= link_to "Sign Up", new_user_registration_path %> or <%= link_to "Sign In", new_user_session_path %></p>
        <% end %>
        
      </div>
    </nav>

  
    <%= yield %>

  </body>
</html>
