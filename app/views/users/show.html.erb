<div class="centering">
  <h2><%= @user.email %></h1>

  <% @user_posts.each do |user_post| %>
      <div class="col-md-4">
        <div class="thumbnail">
          <%= link_to (image_tag user_post.image.url(:medium)), post_path(user_post) %>
       
          <% if user_post.user == current_user %>
            <p><%= link_to 'Edit', edit_post_path(user_post) %> | 
               <%= link_to 'Destroy', post_path(user_post), method: :delete, data: { confirm: 'Are you sure?' } %>
            </p>
          <% end %>
           
        </div>
      </div>
  <% end %>
</div>