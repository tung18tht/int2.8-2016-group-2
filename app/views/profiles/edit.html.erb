<div class="newUI">
	<div class="logform">
	<h1>Editing Profile</h1>

	<%= image_tag @profile.avatar.url(:medium) %>
	<%= render 'form' %>

	<%= link_to 'Back', user_path(current_user)  %>
	</div>
</div>